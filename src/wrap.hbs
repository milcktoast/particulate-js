// ..................................................
// Particulate.js
//
// version : 0.3.2
// authors : Jay Weeks
// license : MIT
// particulatejs.org
// ..................................................

(function (root, factory) {
  if (typeof define === 'function' && define.amd) {
    define('{{amdModuleId}}', [{{{amdDependencies.wrapped}}}], function ({{{amdDependencies.params}}}) {
      return (root['{{{globalAlias}}}'] = factory({{{amdDependencies.params}}}));
    });
  } else if (typeof exports === 'object') {
    module.exports = factory({{{cjsDependencies.wrapped}}});
  } else {
    root['{{{globalAlias}}}'] = factory({{{globalDependencies.normal}}});
  }
}(this, function ({{dependencies}}) {
  'use strict';
  var {{{objectToExport}}};

{{{code}}}

  return {{{objectToExport}}};
}));
